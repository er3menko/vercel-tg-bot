# Vercel Telegram Bot

Простой телеграм-бот на Golang для развертывания на Vercel с использованием Serverless Functions. Бот отвечает на любое сообщение пользователя фразой "Hello, I'm Vercel Bot!".

## Возможности

- 🚀 Развертывание на Vercel за минуты
- 📨 Обработка вебхуков Telegram
- ⚡ Serverless архитектура на Go
- 🔒 Безопасное хранение токена через переменные окружения

## Требования

- Аккаунт [Vercel](https://vercel.com)
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))
- [Vercel CLI](https://vercel.com/docs/cli) (опционально, для локальной разработки)

## Быстрый старт

### 1. Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и выберите имя для бота
4. Сохраните полученный **Bot Token** (он понадобится позже)

### 2. Деплой на Vercel

#### Вариант A: Деплой через Web UI

1. Форкните или загрузите этот репозиторий на GitHub
2. Зайдите на [vercel.com](https://vercel.com) и импортируйте проект
3. В настройках проекта добавьте переменную окружения:
   - Name: `TELEGRAM_BOT_TOKEN`
   - Value: ваш токен от BotFather
4. Нажмите "Deploy"
5. После деплоя скопируйте URL вашего проекта (например, `https://your-project.vercel.app`)

#### Вариант B: Деплой через CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой проекта
vercel

# Добавьте переменную окружения
vercel env add TELEGRAM_BOT_TOKEN
# Введите ваш токен бота

# Повторно задеплойте с новой переменной
vercel --prod
```

### 3. Настройка вебхука

После успешного деплоя нужно настроить вебхук в Telegram. Выполните один из вариантов:

#### Вариант A: Через curl

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-project.vercel.app/api/webhook"}'
```

Замените:
- `<YOUR_BOT_TOKEN>` на ваш токен бота
- `your-project.vercel.app` на URL вашего проекта

#### Вариант B: Через браузер

Откройте в браузере:
```
https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://your-project.vercel.app/api/webhook
```

### 4. Проверка

1. Откройте вашего бота в Telegram
2. Отправьте любое сообщение
3. Бот должен ответить: "Hello, I'm Vercel Bot!"

## Структура проекта

```
vercel-tg-bot/
├── api/
│   └── webhook.go          # Serverless функция для обработки вебхуков
├── go.mod                  # Go модуль
├── vercel.json            # Конфигурация Vercel
├── .gitignore
└── README.md
```

## Как это работает

1. Telegram отправляет POST запрос на эндпоинт `/api/webhook` при получении нового сообщения
2. Vercel вызывает serverless функцию `Handler` из `api/webhook.go`
3. Функция парсит входящее сообщение
4. Отправляет ответ "Hello, I'm Vercel Bot!" через Telegram Bot API
5. Возвращает HTTP 200 OK

## Разработка

### Локальный запуск

```bash
# Установите зависимости
go mod download

# Запустите локально с Vercel Dev
vercel dev

# Теперь ваш бот доступен на http://localhost:3000
```

### Тестирование вебхука локально

Для тестирования локально можно использовать [ngrok](https://ngrok.com/):

```bash
# Запустите ngrok
ngrok http 3000

# Используйте полученный URL для настройки вебхука
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-ngrok-url.ngrok.io/api/webhook"}'
```

## Расширение функционала

Чтобы добавить больше команд или логики, отредактируйте `api/webhook.go`. Например:

```go
// Добавьте обработку команд
if update.Message.Text == "/start" {
    sendMessage(botToken, update.Message.Chat.ID, "Добро пожаловать!")
    return
}

// Или проверяйте текст сообщения
if strings.Contains(update.Message.Text, "привет") {
    sendMessage(botToken, update.Message.Chat.ID, "Привет!")
    return
}
```

## Отладка

### Проверка статуса вебхука

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

### Удаление вебхука

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook"
```

### Логи в Vercel

Перейдите в [Vercel Dashboard](https://vercel.com/dashboard) → ваш проект → Functions → выберите функцию → посмотрите логи

## Лимиты

- Vercel Free tier: 100GB bandwidth, 100 часов выполнения функций в месяц
- Telegram Bot API: без официальных лимитов, но рекомендуется не более 30 сообщений в секунду

## Безопасность

- ✅ Токен бота хранится в переменных окружения
- ✅ HTTPS обязателен для вебхуков Telegram
- ⚠️ Рекомендуется добавить проверку подписи Telegram для дополнительной безопасности

## Лицензия

MIT

## Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Vercel Documentation](https://vercel.com/docs)
- [Vercel Go Runtime](https://vercel.com/docs/functions/serverless-functions/runtimes/go)

